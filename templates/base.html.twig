<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Sistema de Gestión de Siniestros Viales{% endblock %}</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}{% endblock %}
        {% block javascripts %}{% endblock %}
    </head>

    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow mb-4">
            <div class="container-fluid">
                <a class="navbar-brand fw-bold" href="/">🚗 Siniestros Viales</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    {% if app.user %}
                        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') starts with 'persona' %}active{% endif %}" href="{{ path('persona_index') }}">👤 Personas</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') starts with 'siniestro' %}active{% endif %}" href="{{ path('siniestro_index') }}">🚨 Siniestros</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') starts with 'detalle' %}active{% endif %}" href="{{ path('detalle_index') }}">🧾 Detalles</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link {% if app.request.get('_route') starts with 'dashboard' %}active{% endif %}" href="{{ path('dashboard_index') }}">📊 Dashboard</a>
                            </li>
                        </ul>

                        <ul class="navbar-nav ms-auto">
                            <li class="nav-item d-flex align-items-center me-3 text-light">
                                Bienvenido, <strong class="ms-1">{{ app.user.username }}</strong>
                            </li>

                            {% if is_granted('ROLE_ADMIN') %}
                                <li class="nav-item">
                                    <span class="badge bg-danger me-3">Administrador</span>
                                </li>
                            {% else %}
                                <li class="nav-item">
                                    <span class="badge bg-primary me-3">Operador</span>
                                </li>
                            {% endif %}

                            <li class="nav-item">
                                <a href="{{ path('logout') }}" class="btn btn-outline-light btn-sm">Cerrar sesión</a>
                            </li>
                        </ul>
                    {% endif %}
                </div>
            </div>
        </nav>

        <div class="container">
            {% block body %}{% endblock %}
        </div>
    </body>
</html>
